#Path to root directory of the project
root.dir=${basedir}/../..
#Path to interfaces directory
interfaces.dir=${root.dir}/_Interfaces
#Directory to store compile (and archived) components (aka delivery.build.dir)
build.dir=${root.dir}/localbuild